#!/usr/bin/node
const Square = require('./5-square');

const s1 = new Square(4);
s1.print(); /* Print the square */
s1.double(); /* Double the size of the square */
s1.print(); /* Print the square again */
