#!/usr/bin/node

/**
 * Import the `add` function from 13-add.js
 */
const add = require('./13-add').add;

/**
 * Call the add function with 3 and 5 as arguments and print the result
 */
console.log(add(3, 5));
